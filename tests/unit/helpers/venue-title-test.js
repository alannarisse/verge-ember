
import { venueTitle } from 'verge-ember/helpers/venue-title';
import { module, test } from 'qunit';

module('Unit | Helper | venue title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = venueTitle([42]);
  assert.ok(result);
});

